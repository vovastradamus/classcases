import { Sheet } from "./sheet";

const flexCase = {};
/**
 *
 * @param {Sheet} sheet
 */
flexCase.install = function install(sheet) {
  const { bindStyleCases } = sheet;
};

export default flexCase;
