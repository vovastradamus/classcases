import { version } from "../package.json";

export const PLUGIN_PREFIX = "ss";
export const PLUGIN_ATTR = "classcase";
export const PLUGIN_ATTR_VERSION = "classcase-version";
export const PLUGIN_VERSION = version;
